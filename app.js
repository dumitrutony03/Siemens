// // app.js

import QuizApp from './quizApp.js';

function init() {
  // Initialize the QuizApp
  const quizApp = new QuizApp();  
}

document.addEventListener('DOMContentLoaded', init);


export default init; // Ensure that init is exported if needed
